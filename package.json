{
  "name": "device-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "bridge": "src/cli.mjs"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests -p 'test_*.py' -v",
    "check": "node --check src/cli.mjs && node --check src/readiness.mjs && node --check src/gateway.mjs && node --check src/transport.mjs && node --check src/pairing.mjs && python3 -m py_compile device/helper.py device/owner.py scripts/prepare_executor.py scripts/review_ui_package.py scripts/install_gateway_config.py",
    "doctor": "node src/cli.mjs doctor"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.1.13"
  }
}
